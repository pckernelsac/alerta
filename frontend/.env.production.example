# Copia a .env.production o configura las mismas variables en CI / panel del hosting.
# Vite solo expone variables que empiezan por VITE_ y se sustituyen en build time.

VITE_API_URL=https://api.ejemplo.com
